p
  strong
    - if user == message.user
      = user.full_name
    - elsif user == message.manager
      ' Client
    - elsif user == message.user
      ' TaskForce Agent
  ' :
  - if message.image.present?
    = image_tag message.image.url(:thumb)
  - else
    = message.body