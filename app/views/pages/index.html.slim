#example.index
  .following.bar
    .ui.container
      .ui.large.secondary.network.menu.inverted
        .right.menu.inverted
          - if user_signed_in?
            = link_to "Sign Out", destroy_user_session_path, method: :delete, class: "item"
            br

  .masthead.segment.bg7
    .ui.container
      .introduction
        span.library = image_tag "logo-invert.png", style: "height: 150px", alt: "Goomp Logo"
        h1.ui.inverted.header
          span.tagline The AI bot that helps your work done through real people!
        = render "goomp_bot/new_user"
        .ui.hidden.divider
        = render "social_buttons_modal", modal_id: "social-buttons-modal"
